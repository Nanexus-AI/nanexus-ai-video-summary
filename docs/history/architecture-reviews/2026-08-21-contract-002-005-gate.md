# CONTRACT-002～005 Architecture Gate

> **Historical architecture record.** Retained for engineering context; current architecture is
> documented in [`docs/architecture.md`](../../architecture.md).

Status: **APPROVED AND FROZEN**
Date: 2026-08-21
Authorization: product owner explicitly requested complete design and freeze of CONTRACT-002～005.

Event Intelligence owns the normative Result/Capability schemas. Video Summary contains an independent consumer mirror and a byte-identical sanitized fixture, with no runtime import or reverse dependency. Result status, invocation facts, typed claims, short auditable errors, Capability version/support/media limits, default-deny object-scoped authority, Replay/Dry Run behavior, and major/minor compatibility rules have no unresolved v1 semantics.

Both repositories independently validate Job, Result and Capability. The consumer rejects incompatible versions and forbidden URLs, credentials and chain-of-thought. No database/API/queue/Worker/Evidence implementation or old-chain change is authorized by this Gate.

Stage 1 exit conditions are satisfied. Stage 2 is not started.
