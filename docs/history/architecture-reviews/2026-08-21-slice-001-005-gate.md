# SLICE-001-005 cross-repository acceptance

> **Historical architecture record.** Retained for engineering context; current architecture is
> documented in [`docs/architecture.md`](../../architecture.md).

- Date: 2026-08-21
- Status: implementation and isolated integration tests passed; stage 4 not started

## Result

SLICE-001 adds an independent HTTP client for Health, Capability, Job, Subject, Evidence and Result with timeout, retry classification, structured errors, version checks and redacted logs. SLICE-002 adds the external deterministic Stub Worker. SLICE-003 uses a Bearer service identity with job-scoped Subject, Evidence and Result authority. SLICE-004 adds compose.slice.yaml with an independent Event Intelligence database. SLICE-005 covers incompatible versions, missing capability, duplicate Result and Evidence denial in both repositories.

## Boundary

Video Summary imports no Event Intelligence package, ORM or database and reads no Frigate URL, credential or internal path. Evidence crosses only the Processor API. Event Intelligence has no reverse dependency. Legacy MQTT, Event, AI Worker, Search, Summary and Chat paths remain intact.

## Verification

- Video Summary: 20 passed; compileall and git diff --check passed.
- Event Intelligence: 152 passed, 3 external-service tests skipped; Ruff and Mypy passed; Processor API 3 passed.
- Compose v1 config validation passed.

## Rollback and prerequisites

Not enabling compose.slice.yaml leaves both existing runtime paths unchanged. Event Intelligence defaults to internal_stub; only the overlay selects external. Before stage 4, run the real Compose smoke test with a Docker daemon, a non-example token and working Frigate media configuration. Do not start later migration automatically.
