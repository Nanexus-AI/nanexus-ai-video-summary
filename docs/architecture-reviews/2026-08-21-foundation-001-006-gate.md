# FOUNDATION-001～006 Cross-Repository Gate

Status: **APPROVED — STAGE 2 COMPLETE**
Date: 2026-08-21
Scope: Event Intelligence AI Enrichment foundation only; stage 3 is not started.

## Verified implementation

The Event Intelligence repository owns and implements the stage 2 foundation. Alembic revision `4b1f001006` adds ProcessorJob/Attempt persistence, ModelInvocation lifecycle/error fields, Claim relationships, idempotency and status indexes. Frigate review ingest maintains ReviewItem/ReviewObservation and atomically creates the ended-revision Job plus Outbox record. Missing snapshot Evidence produces an audited abstention.

AI enrichment uses its own Redis consumer group and DLQ. Retry, terminal DLQ and operator requeue update ProcessorJob, ProcessorJobAttempt and AuditRecord without claiming a distributed database/Redis transaction. The deterministic local-only Stub writes one auditable Claim and ModelInvocation; duplicate delivery is idempotent. Event detail API and Web UI expose source facts separately from AI interpretation, model identity, status, Evidence and failure/abstention state.

## Boundary review

- Dependency remains `Video Summary → Event Intelligence`; Event Intelligence does not import Video Summary.
- No database, internal ORM, Redis key or internal filesystem is shared across repositories.
- Processor envelopes contain opaque Evidence identifiers, not Frigate URLs or credentials.
- Video Summary does not access Frigate or Evidence in this stage.
- No real model, cloud call, notification/writeback, publication, push or legacy-chain deletion was introduced.

## Verification evidence

- Event Intelligence local isolated backend: `149 passed, 3 skipped`; skips require PostgreSQL/Redis.
- Event Intelligence with real PostgreSQL/Redis, including kill/restart recovery: `152 passed`, no skips.
- Ruff: passed.
- Mypy strict: passed for 72 source files.
- Alembic: one head, `4b1f001006`.
- Web: 5 tests passed; production build passed.
- Retry/DLQ coverage includes pending recovery across worker restart, timeout retry to DLQ, poison-message immediate DLQ, duplicate delivery, unavailable Evidence and atomic DLQ requeue.
- Existing Outbox tests cover the committed unpublished row and temporary Redis failure. Replay remains local/dry and does not invoke a real provider.

The repository acceptance records the executed PostgreSQL/Redis kill/restart procedure, retained Redis AOF marker and retained Alembic revision. Together with deterministic fault tests, this satisfies the stage 2 reliability Gate.

## Exit decision

Stage 2 exit conditions are satisfied. Stage 3 requires separate authorization and must start with prerequisite and contract-boundary verification; it has not been started by this Gate.
