export type Feature={available:boolean;mode?:string;asynchronous:boolean};
export type Capabilities={api_version:string;subject_reference:string;subject_path_template:string;summary:Feature;search:Feature;chat:Feature;legacy_fallback_available:boolean;ownership_authentication:string};
export type Summary={content:string;local_date:string;generator:string;source_subject_ids:string[]};
export type SearchHit={subject_id:string;score:number;camera?:string;labels:string[];occurred_at?:string;subject_path:string};
export type ChatJob={id:string;conversation_id:number;status:string;error_code?:string;answer?:{content:string;method?:string;degraded:boolean;error_code?:string;citations:{subject_id:string;review_path:string}[]}};
const json=async<T>(path:string,init?:RequestInit):Promise<T>=>{const r=await fetch(path,{...init,headers:{'Content-Type':'application/json',...(init?.headers??{})}});if(!r.ok)throw new Error(`${r.status}: ${await r.text()}`);return r.json()};
export const api={capabilities:()=>json<Capabilities>('/api/v1/capabilities'),health:()=>json<{status:string;database:boolean;redis:boolean;ai_mode:string;summary_mode:string;chat_mode:string}>('/health'),summary:(day:string,tz:string)=>json<{summary:Summary|null}>(`/api/v1/summaries/${day}?timezone=${encodeURIComponent(tz)}`),search:(query:string)=>json<{items:SearchHit[];degraded:boolean;degradation_reason?:string}>('/api/v1/search',{method:'POST',body:JSON.stringify({query})}),chat:(message:string,owner_id:string,conversation_id?:number)=>json<ChatJob>('/api/v1/chat/jobs',{method:'POST',body:JSON.stringify({message,owner_id,conversation_id,timezone:Intl.DateTimeFormat().resolvedOptions().timeZone})}),job:(id:string,owner:string)=>json<ChatJob>(`/api/v1/chat/jobs/${id}?owner_id=${encodeURIComponent(owner)}`)};
export const subjectHref=(id:string)=>`/api/v1/subjects/${encodeURIComponent(id)}`;
